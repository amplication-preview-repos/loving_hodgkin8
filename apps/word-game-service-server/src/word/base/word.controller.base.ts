/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { WordService } from "../word.service";
import { WordCreateInput } from "./WordCreateInput";
import { Word } from "./Word";
import { WordFindManyArgs } from "./WordFindManyArgs";
import { WordWhereUniqueInput } from "./WordWhereUniqueInput";
import { WordUpdateInput } from "./WordUpdateInput";
import { GuessFindManyArgs } from "../../guess/base/GuessFindManyArgs";
import { Guess } from "../../guess/base/Guess";
import { GuessWhereUniqueInput } from "../../guess/base/GuessWhereUniqueInput";

export class WordControllerBase {
  constructor(protected readonly service: WordService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Word })
  async createWord(@common.Body() data: WordCreateInput): Promise<Word> {
    return await this.service.createWord({
      data: data,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        description: true,
        word: true,
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [Word] })
  @ApiNestedQuery(WordFindManyArgs)
  async words(@common.Req() request: Request): Promise<Word[]> {
    const args = plainToClass(WordFindManyArgs, request.query);
    return this.service.words({
      ...args,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        description: true,
        word: true,
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Word })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async word(
    @common.Param() params: WordWhereUniqueInput
  ): Promise<Word | null> {
    const result = await this.service.word({
      where: params,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        description: true,
        word: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Word })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async updateWord(
    @common.Param() params: WordWhereUniqueInput,
    @common.Body() data: WordUpdateInput
  ): Promise<Word | null> {
    try {
      return await this.service.updateWord({
        where: params,
        data: data,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
          description: true,
          word: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Word })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async deleteWord(
    @common.Param() params: WordWhereUniqueInput
  ): Promise<Word | null> {
    try {
      return await this.service.deleteWord({
        where: params,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
          description: true,
          word: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Get("/:id/guesses")
  @ApiNestedQuery(GuessFindManyArgs)
  async findGuesses(
    @common.Req() request: Request,
    @common.Param() params: WordWhereUniqueInput
  ): Promise<Guess[]> {
    const query = plainToClass(GuessFindManyArgs, request.query);
    const results = await this.service.findGuesses(params.id, {
      ...query,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        isCorrect: true,

        game: {
          select: {
            id: true,
          },
        },

        word: {
          select: {
            id: true,
          },
        },
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/guesses")
  async connectGuesses(
    @common.Param() params: WordWhereUniqueInput,
    @common.Body() body: GuessWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      guesses: {
        connect: body,
      },
    };
    await this.service.updateWord({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/guesses")
  async updateGuesses(
    @common.Param() params: WordWhereUniqueInput,
    @common.Body() body: GuessWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      guesses: {
        set: body,
      },
    };
    await this.service.updateWord({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/guesses")
  async disconnectGuesses(
    @common.Param() params: WordWhereUniqueInput,
    @common.Body() body: GuessWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      guesses: {
        disconnect: body,
      },
    };
    await this.service.updateWord({
      where: params,
      data,
      select: { id: true },
    });
  }
}
